[build]
  base = "frontend"
  publish = "frontend/build"
  command = "cd frontend && npm install && npm run build"

[build.environment]
  NODE_VERSION = "16.x"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 